// Create a new string variable named greeting and assign it the value "Good" concatenated with singleQuotesString and then "Morning!".

let greet1 = "good";
let greet2 = "morning";
console.log(`concatenated string is: ${greet1} ${greet2}`);

// Create a new string variable named newString and assign it any text you like.
let newString = "this is my new string!";
// Calculate and store the length of newString in a variable called newStringLength.
let newStringLength = newString.length;
console.log(newStringLength);
